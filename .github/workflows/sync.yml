name: Sync

on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 2/4 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Sync upstream
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote add upstream https://github.com/Hiro420/StellaSoraData.git
          git fetch upstream
          git checkout main
          git merge upstream/main -X theirs --no-edit || {
            echo "Merge conflict detected. Manual resolution required.";
            exit 1;
          }

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      # We only need runtime, but this action also does caching so should save time
      - name: Install .NET 9 Runtime
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "9.0.x"

      - name: Run scripts
        run: |
          bun install
          bun run generate

      - name: Push any changes
        run: |
          git add --all
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git commit -m "Synced with upstream"
          git push origin main
